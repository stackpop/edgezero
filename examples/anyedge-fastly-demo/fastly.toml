name = "anyedge-fastly-demo"
description = "AnyEdge + Fastly adapter demo"
authors = ["Your Name <you@example.com>"]
language = "rust"

# Optional: set this after creating a service or pass --service-id on CLI
# service_id = ""

[local_server]
  [local_server.backends]
  # Define backends here if your app uses them. This demo does not.

