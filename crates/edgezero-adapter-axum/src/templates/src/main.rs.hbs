use {{proj_core_mod}}::App;

fn main() {
    if let Err(err) = edgezero_adapter_axum::dev_server::run_app::<App>(include_str!("../../../edgezero.toml")) {
        eprintln!("axum adapter failed: {err}");
        std::process::exit(1);
    }
}
