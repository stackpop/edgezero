[package]
name = "{{proj_fastly}}"
version = "0.1.0"
edition = "2021"
publish = false

[[bin]]
name = "{{proj_fastly}}"
path = "src/main.rs"

[features]
default = []
fastly = ["anyedge-adapter-fastly/fastly"]

[dependencies]
{{proj_core}} = { path = "../{{proj_core}}" }
log = { workspace = true }
{{{dep_anyedge_adapter_fastly}}}

[target.'cfg(target_arch = "wasm32")'.dependencies]
{{{dep_anyedge_core_fastly}}}
{{{dep_anyedge_adapter_fastly_wasm}}}
fastly = { workspace = true }
once_cell = { workspace = true }
