[package]
name = "anyedge-std"
version = "0.1.0"
edition = "2021"
description = "Stdout/stderr logger adapter for AnyEdge (non-provider)"

[dependencies]
anyedge-core = { path = "../anyedge-core" }
log = "0.4"
fern = "0.7"
chrono = { version = "0.4", features = ["clock"] }

