[package]
name = "{{proj_cloudflare}}"
version = "0.1.0"
edition = "2021"
publish = false

[[bin]]
name = "{{proj_cloudflare}}"
path = "src/main.rs"

[features]
default = ["cloudflare"]
cloudflare = ["anyedge-adapter-cloudflare/cloudflare"]

[dependencies]
{{proj_core}} = { path = "../{{proj_core}}" }
{{dep_anyedge_adapter_cloudflare}}
anyedge-core = { workspace = true }
worker = { workspace = true }

[target.'cfg(target_arch = "wasm32")'.dependencies]
{{dep_anyedge_core_cloudflare}}
{{dep_anyedge_adapter_cloudflare_wasm}}
