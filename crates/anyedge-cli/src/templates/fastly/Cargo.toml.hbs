[package]
name = "{{proj_fastly}}"
version = "0.1.0"
edition = "2021"
publish = false

[[bin]]
name = "{{proj_fastly}}"
path = "src/main.rs"

[dependencies]
{{proj_core}} = { path = "../{{proj_core}}" }
{{dep_anyedge_fastly}}
log = "0.4"

[target.'cfg(target_arch = "wasm32")'.dependencies]
fastly = "0.11"

