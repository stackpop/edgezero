[package]
name = "{{proj_fastly}}"
version = "0.1.0"
edition = "2021"
publish = false

[[bin]]
name = "{{proj_fastly}}"
path = "src/main.rs"

[dependencies]
{{proj_core}} = { path = "../{{proj_core}}" }
log = "0.4"

[target.'cfg(target_arch = "wasm32")'.dependencies]
{{dep_anyedge_core_fastly}}
{{dep_anyedge_adapter_fastly}}
fastly = "0.11"
