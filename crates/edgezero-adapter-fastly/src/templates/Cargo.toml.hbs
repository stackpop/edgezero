[package]
name = "{{proj_fastly}}"
version = "0.1.0"
edition = "2021"
publish = false

[[bin]]
name = "{{proj_fastly}}"
path = "src/main.rs"

[features]
default = []
fastly = ["edgezero-adapter-fastly/fastly"]

[dependencies]
{{proj_core}} = { path = "../{{proj_core}}" }
{{{dep_edgezero_adapter_fastly}}}
log = { workspace = true }

[target.'cfg(target_arch = "wasm32")'.dependencies]
{{{dep_edgezero_adapter_fastly_wasm}}}
{{{dep_edgezero_core_fastly}}}
fastly = { workspace = true }
once_cell = { workspace = true }
